touch .gitignore
git config --global user.name Subham
git config --global user.email subham58kvb@gmail.com
git init
git add .
git commit -m "Initial Commit"
git status # Just to see
ssh-keygen -t rsa -b 4096 -C "subham58kvb@gmail.com"
tail <path to id_rsa.pub> #paste this to your github account
git remote add origin <ssh url.git>
git push origin master